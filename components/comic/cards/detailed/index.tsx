import React from "react";

import * as Styled from "./styles";

const Card: React.FC = () => {
    return (
        <>
            <Styled.Card>
                <Styled.Splash />
            </Styled.Card>
        </>
    );
};

export default Card;
